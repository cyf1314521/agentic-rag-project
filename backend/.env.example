# Milvus Configuration
# MILVUS_URI=./db/milvus.db         # Milvus Lite (local file, no server needed)
MILVUS_URI=http://localhost:19530  # Milvus standalone server
COLLECTION_NAME=papers

# Model Paths
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# Retrieval Parameters
FETCH_K=20
TOP_K=5
RRF_K=60

# Cache Configuration
ENABLE_CACHE=true
CACHE_MAX_SIZE=1000

# LLM Configuration
LLM_BASE_URL=http://localhost:8848/v1
LLM_MODEL=GPT-4o-mini
LLM_API_KEY=empty
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4096
MAX_RETRIES=0

# VLM Configuration
VLM_ENABLED=true
VLM_BASE_URL=http://localhost:8080/v1
VLM_MODEL=Qwen3-vl-4B
VLM_API_KEY=empty

# Postgres (checkpointer + session store)
POSTGRES_URI=postgresql://postgres:postgres@localhost:5432/scholar_rag

# Upload
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=50

# Server
HOST=0.0.0.0
PORT=8000
